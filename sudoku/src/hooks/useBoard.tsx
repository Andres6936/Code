import {Coordinate} from "../types/Coordinate";

export function useBoard(board: number[]) {
    const getZone = ({x, y}: Coordinate): number[] => {

    }

    const getCell = ({x, y}: Coordinate): number => {

    }

    return {getZone, getCell}
}